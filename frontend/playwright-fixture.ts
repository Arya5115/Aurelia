// Re-export the base fixture from Playwright's test runner. If you had
// custom fixtures provided by an internal package, re-implement them here
// or add a small wrapper module instead.
export { test, expect } from '@playwright/test';
